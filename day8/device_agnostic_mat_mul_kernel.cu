// We will be using the extern keyword and combining Mds and Nds into a linear array to get 
// a kernel that can adjust according to the size of the shared memory and memory/thread on the CUDA device (GPU)


// TODO: write this after testing the general mat mul file


